# ============================================================
# PostgreSQL Server Configuration
# ============================================================
# Tuned for a general-purpose EC2 instance (2-4 vCPUs, 4-8 GB RAM).
# Adjust values based on your instance size.
# ============================================================

# --- Connection Settings ---
listen_addresses = '*'
port = 5432
max_connections = 200

# --- Memory ---
# Set shared_buffers to ~25% of available RAM
shared_buffers = 1GB
# Set effective_cache_size to ~75% of available RAM
effective_cache_size = 3GB
# Work memory per sort/hash operation
work_mem = 16MB
# Memory for maintenance operations (VACUUM, CREATE INDEX)
maintenance_work_mem = 256MB

# --- Write Ahead Log (WAL) ---
wal_level = replica
max_wal_senders = 5
wal_keep_size = 1GB
checkpoint_timeout = 15min
checkpoint_completion_target = 0.9

# --- Query Planner ---
random_page_cost = 1.1
effective_io_concurrency = 200
default_statistics_target = 100

# --- Logging ---
logging_collector = on
log_directory = 'log'
log_filename = 'postgresql-%Y-%m-%d.log'
log_rotation_age = 1d
log_rotation_size = 100MB
log_min_duration_statement = 1000
log_checkpoints = on
log_connections = on
log_disconnections = on
log_lock_waits = on
log_temp_files = 0
log_timezone = 'UTC'

# --- Locale & Encoding ---
datestyle = 'iso, mdy'
timezone = 'UTC'
lc_messages = 'en_US.utf8'
lc_monetary = 'en_US.utf8'
lc_numeric = 'en_US.utf8'
lc_time = 'en_US.utf8'
default_text_search_config = 'pg_catalog.english'

# --- Autovacuum ---
autovacuum = on
autovacuum_max_workers = 3
autovacuum_naptime = 60
autovacuum_vacuum_threshold = 50
autovacuum_analyze_threshold = 50
